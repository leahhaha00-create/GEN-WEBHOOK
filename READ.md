# Discord Webhook Bot

A Discord bot that automatically:

✔ Creates all required channels with aesthetic emoji names  
✔ Generates all webhooks  
✔ Saves all webhook URLs inside a “📁┃saved-webhook” channel  
✔ Supports `/gen` command to get specific webhook  

## 🚀 Run Locally
